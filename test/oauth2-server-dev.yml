server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          searchPaths: respo
          uri: https://github.com/AndeyJiang/cloud-config/
      label: master
  application:
    name: config-server

